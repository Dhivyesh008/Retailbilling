import {X} from 'lucide-react'; import {useState} from 'react'; import {useStore} from '../context/StoreContext'; export default function ProductModal({close}){const {addProduct}=useStore();const [f,setF]=useState({name:'',sku:'',category:'Grocery',price:'',tax:5,stock:'',reorder:''});const set=e=>setF({...f,[e.target.name]:e.target.value});return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/40 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6"><div className="mb-5 flex justify-between"><h2 className="text-lg font-bold">Add product</h2><button onClick={close}><X/></button></div><div className="grid grid-cols-2 gap-3">{[['name','Product Name'],['sku','SKU'],['category','Category'],['price','Price'],['tax','Tax %'],['stock','Stock Quantity'],['reorder','Reorder Level']].map(([n,l])=><label className="text-sm font-bold text-slate-600" key={n}>{l}<input className="field mt-1" name={n} value={f[n]} onChange={set} type={['price','tax','stock','reorder'].includes(n)?'number':'text'}/></label>)}</div><button className="btn-primary mt-5 w-full" onClick={()=>{if(f.name&&f.sku){addProduct(f);close()}}}>Save product</button></div></div>}
