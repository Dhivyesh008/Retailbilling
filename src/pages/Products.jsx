import {Plus,Search} from 'lucide-react'; import {useState} from 'react'; import {useStore} from '../context/StoreContext'; import ProductTable from '../components/ProductTable'; import ProductModal from '../components/ProductModal'; export default function Products(){const {products}=useStore();const [q,setQ]=useState(''),[open,setOpen]=useState(false);const list=products.filter(p=>(p.name+p.sku+p.category).toLowerCase().includes(q.toLowerCase()));return <><div className="mb-7 flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">Catalog management</p><h1 className="mt-1 text-2xl font-extrabold">Products</h1><p className="text-sm text-slate-500">Manage your product catalog and pricing.</p></div><button className="btn-primary" onClick={()=>setOpen(true)}><Plus size={17}/>Add product</button></div><section className="card"><div className="flex flex-wrap justify-between gap-3 border-b border-slate-100 p-5"><div className="relative"><Search size={17} className="absolute left-3 top-2.5 text-slate-400"/><input className="field w-72 pl-9" placeholder="Search products..." value={q} onChange={e=>setQ(e.target.value)}/></div><span className="text-sm text-slate-500">{products.length} products</span></div><ProductTable products={list}/></section>{open&&<ProductModal close={()=>setOpen(false)}/>}</>}
